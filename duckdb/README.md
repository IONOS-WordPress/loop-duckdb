- The `loop-duckdb.db*` database contains the entire set of IONOS Loop data.

- The `ui.db*` database contains the DuckDB notebooks.

The populated lop-duckdb database can be downloaded from and uploaded to S3.
